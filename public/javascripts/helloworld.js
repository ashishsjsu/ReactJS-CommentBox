//HTML tags with JSX
React.render(

	<p>This HTML element is generated by React</p>,
	document.getElementById('container')
);


//React components with JSX
//React components are functions that take props and states, and render html
var HelloReact = React.createClass({
	render: function(){
		return (
			<p>
				Hello, <input type="text" placeholder="Your name here" />!

				<p> It is {this.props.date.toTimeString()}</p>	
			</p>
		);
	}
});



setInterval(function(){

	var MyElement = <HelloReact date={new Date()} />;
 	
 	//what component and where
 	React.render(MyElement, document.getElementById('container2'));

}, 500);


// React props demo
//use this.props to evaluate the vars passed in while rendering
var Profile = React.createClass({

	render: function(){
		return (
			<div>
				<p>I am {this.props.name}, I study at {this.props.school}</p>
			</div>
		);
	}
});


React.render(<Profile name="John" school="SFSU" />, document.getElementById('profile'))


// React children demo
var MenuItem = React.createClass({

	//this is the React components event, we will call this whem link is clicked
	printDescription: function(){
		
		alert(this.props.children);		
	},
		
	render: function(){
		
		return (
			<div>
				<h3>{this.props.title}</h3>
				<a href="#" onClick={this.printDescription}>Whats this?</a>
				<p>{this.props.children}</p>
			</div>
		);
	}
});


var menu = <div><MenuItem title="Cheesburger">A juicy 100% beef patty simply seasoned with a pinch of salt and pepper, melty American cheese, tangy pickles, minced onions, ketchup and mustard.
</MenuItem><MenuItem title="Grilled Chicken Sandwich">100% grilled chicken breast filet seasoned to perfection with ingredients like salt, garlic and parsley – seared in our kitchens, no preservatives added. Crisp leaf lettuce, fresh tomato, and a vinaigrette dressing. All atop our delectable artisan roll.</MenuItem></div>

React.render(menu, document.getElementById('menu'));


//React dynamic UI and reactivity

var LikeButton = React.createClass({

	getInitialState: function(){
		return { liked: false };
	},
	handleClick: function(){
		this.setState({liked: !this.state.liked});
	},

	render: function(){
		var text = this.state.liked ? 'like' : 'haven\'t liked';
		
		return (
			<p><i className="fa fa-thumbs-o-up" onClick={this.handleClick}></i>
			You {text} this			
			</p>
		);
	}
});

React.render(
	<LikeButton />,
	document.getElementById('likeItem')
);